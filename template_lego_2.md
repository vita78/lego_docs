# Название блока 

[Github](ссылка на директорию блока)

> **NB** API блока нестабильно («unstable») до снятия метки. ( *для блока с нестабильным API* )

> **NB** Блок устарел («deprecated»), в версии x.x будет удален [в пользу блока [name_newblock](#name_newblock.md)]. ( *для устаревшего блока или версии блока* )

Блок предоставляет <*Выберите нужный вариант*>

Блок-помощник представляет
        
>компонент интерфейса для ...  
 шаблон, создающий набор HTML-элементов ...  
 СSS-класс, реализующий ...  
 набор JS-классов, реализующий ...

 Используется только в составе блока [name_ownerblock](#name_ownerblock) 
 (*для вспомогательных блоков*) ...

<[Краткое описание блока](guideline.md/#block-definition)>
 
>*Для новых версий блоков краткая информация об основных отличиях от предыдущей со ссылкой на раздел с подробным перечислением значимых изменений и причина.*

<Простейший пример блока>

## Обзор

## Модификаторы блока

>*Пример последовательности модификаторов: `type`, `theme`, `size`, `mode`, `disabled`, `focused`, `togglable`, `pressed`, `hovered`.*

>*Допустимые способы использования: `JS`, `BEMJSON`, `—` (нет возможности устанавливать вручную).*

> *Уровень переопределения указывается, если он отличен от `common`. Допустимые значения: `desktop`, `touch-pad`, `touch-phone` (в заданном порядке, после `common`)*. 

| Модификатор | Допустимые значения | Способы использования | Описание |
| ----------- | ------------------- | --------------------- | -------- |
| [Название модификатора](#modifiers-name) (*якорь на соответствующий заголовок* ) | `'значение 1'`, `'значение 2'` | `BEMJSON`, `JS` (*Выберите необходимое значение*) | Краткое описание. Уровень переопределения: `название уровня`. |

### Специализированные поля блока

>*Пример последовательности полей: `name`, `val`, `text`, `url`, `icon`, `title`, `id`, `tabIndex`.*

>*Допустимые типы данных: `String`, `Number`, `Boolean`, `Array`, `BEMJSON`.*

| Поле | Тип | Описание |
| ---- | --- | -------- |
| [Название поля](#fields-name) (*якорь на соответствующий заголовок* | `Тип данных` | Краткое описание. |

### Элементы блока

>*Допустимые способы использования: `BEMJSON`  (другие варианты не предусмотрены).*
> *Уровень переопределения указывается, если он отличен от `common`. Допустимые значения: `desktop`, `touch-pad`, `touch-phone` (в заданном порядке, после `common`)*. 

| Элемент | Описание |
| --------| -------- |
| [Название элемента](#elems-name) (*якорь на соответствующий заголовок*) | Краткое описание. Уровень переопределения: `название уровня`.|

#### Модификаторы элемента блока

>*Пример последовательности модификаторов: `type`, `theme`, `size`, `mode`, `disabled`, `focused`, `togglable`, `pressed`, `hovered`.*

>*Допустимые способы использования: `JS`, `BEMJSON`, `—` (нет возможности устанавливать вручную).*

| Элемент | Модификатор | Допустимые значения | Способы использования | Описание |
| ------- | ----------- | ------------------- | --------------------- | -------- |
| Название элемента, к которому относится описываемый модификатор | [Название модификатора](##elems-name-modifiers-name) (*якорь на соответствующий заголовок* ) | `'значение 1'`, `'значение 2'` | `BEMJSON`, `JS` (*Выберите необходимое значение*) | Краткое описание. |


#### Специализированные поля элементов блока

| Элемент | Поле | Тип | Описание |
| ------- | ---- | --- | -------- |
| Название элемента, к которому относится описываемое поле | [Название поля](#elems-name-fields-name) (*якорь на соответствующий заголовок*) | `Тип данных` | Краткое описание. |


### JS-параметры блока

> **Важно!** Опциональные параметры указываются в квадратных скобках.  
> *Уровень переопределения указывается, если он отличен от `common`. Допустимые значения: `desktop`, `touch-pad`, `touch-phone` (в заданном порядке, после `common`)*. 

| Параметр | Тип | Значение по умолчанию | Описание |
| -------- | --- | --------------------- | -------- |
| [Название параметра](#params-name1) (*якорь на соответствующий заголовок*) |`Тип данных` | `Значение по умолчанию` | Краткое описание. |
| [[Название параметра]](#params-name2) (*якорь на соответствующий заголовок*)| `Тип данных` | `Значение по умолчанию` | Краткое описание. Уровень переопределения: `название уровня`.|


### Публичные технологии блока

Блок реализован в технологиях:

>*Лишнее удалить. Технологии, отсутствующие в списке, не являются публичными и не указываются.*

* `js`
* `bh.js`
* `bemhtml.js`
* `css`
* `svg`

## Подробнее

<[Подробное описание блока](guideline.md/#block-description)>

> *Для составного блока*  
> 
Реализация блока включает в себя блоки:

* [название_блока1](ссылка на блок1)
* [название_блока2](ссылка на блока2)

>*Если функциональность блока основана на типовом элементе формы* 
 
Функциональность блока основана на типовом элементе формы [название элемента](https://ru.wikipedia.org/wiki/Элемент_интерфейса).

Клавиатурное управление:

* **Клавиша** – описание выполняемого действия.

Зависимости блока [deps.js](#)

Особенности реализации: (*при напличии, перечислить ключевые особенности в виде маркированного списка*)

* Деградации в IE версии X.
* ...

<a name="modifiers"></a>
### Модификаторы блока

<a name="modifiers-name"></a>
#### Модификатор `название модификатора`

> В случае, если модификатор и его допустимые значения можно описать лаконично и не нуждаются в подробном пояснении, оформляем данные таблицей.

| Допустимые значения |Значение по умолчанию| Способ использования | Описание |
| ------------------- | ------------------- | -------------------- | ---------|
| `'значение 1'`| `-`  |`BEMJSON`, `JS` (*Выберите необходимое значение*) | Краткое описание. Уровень переопределения: `название уровня`. |


Допустимые значения: `'значение 1'`, `'значение 2'`. *Обратите внимание, строчные значения заключаются в одинарные вертикальные кавычки и бэктики.*

Значение по умолчанию: *Если нет, ставится дефис*.  Обязательный модификатор. *Для необязательных модификаторов признак не указывается*.

Способ использования: `BEMJSON`, `JS`. *Выберите нужное значение.*

Уровень переопределения: *Указывается, если уровень отличен от `common`. Для `common` – удаляется*.

<Описание>
Ограничения или условия при которых используется.

Зависимости: Подключите на уровне сервиса <тип_зависимости>–зависимость от <тип_сущности> `имя_сущности` и его требуемых значений в  `путь_к_файлу_ ` *Указываются для опциональных данных, указать зависимости mustDeps или shoudDeps *

*Описание доопределения/переопределения модификатора и пример для нестандартного случая*
Доопределить на уровне переопределения проекта

<a name="modifiers-name-1"></a>
##### Смысловое название блока с примененным модификатором (модификатор `название модификатора` в значении `значение 1`)

<Описание>

<Пример>

<a name="modifiers-name-2"></a>
##### Смысловое название блока с примененным модификатором (модификатор `название модификатора` в значении `значение 2`)

<Описание>

<Пример>


<a name="modifiers-name"></a>
#### Модификатор `название модификатора`

Допустимое значение: `булево значение`. *Обратите внимание, булевы значения заключаются только в бэктики.*

Способ использования: `BEMJSON`, `JS`. *Выберите нужное значение.*

<Описание>

<Пример>

<a name="declfields"></a>
### Специализированные поля блока

<a name="declfields-field"></a>
#### Поле `название поля`

Тип: `String`, `Number`, `Array`, `Boolean`. *Выберите нужный тип данных. Для полиморфных полей типы разделяются знаком `|`*

Значение по умолчанию: *Если нет, ставится дефис*.  Обязательное поле. *Для необязательных полей признак не указывается*.

Уровень переопределения: *Указывается, если уровень отличен от `common`. Для `common` – удаляется*.

<Описание>

<Пример>

<a name="elems"></a>
### Элементы блока

<a name="elems-name"></a>
#### Элемент `название элемента`

Элемент предоставляет

        функцию, реализующую ...

        объект, содержащий набор методов для ...

        объект, содержащий набор свойств для ...

        объект, содержащий набор свойств и методов для ...

        объект события, дополненный набором методов (свойств) для ...

        шаблон для ...

        набор специализированных полей для ...

        СSS-класс, реализующий ...

        JS-класс, реализующий ...

Элемент дополняет

        объект события набором методов (свойств) для ...

        базовый блок набором методов (свойств) для ...

Обязательный элемент. *Для необязательных элементов признак не указывается*. 

Уровень переопределения: *Указывается, если уровень отличен от `common`. Для `common` – удаляется*.

<Описание> Ограничения или условия при которых используется.

<Пример>

Зависимость: *Зависимости ля опциональных элементов*

*Описание доопределения/переопределения элемента и пример для нестандартного случая*

<a name="elems-name-fields"></a>
### Специализированные поля элементов блока

<a name="elems-name-fields-name"></a>
#### Специализированное поле `название поля` элемента `название элемента`

Тип: `String`, `Number`, `Array`, `Boolean`. *Выберите нужный тип данных.*

Значение по умолчанию: *Если нет, ставится дефис*. Обязательное поле. *Для необязательных полей признак не указывается*.

<Описание>

<Пример>

<a name="declparams"></a>
### JS-параметры блока

<a name="params-name"></a>
#### Поле `название параметра`

Тип: `String`, `Number`, `Array`, `Boolean`. *Выберите нужный тип данных. Для полиморфных параметров типы разделяются знаком `|`*

Значение по умолчанию: *Если нет, ставится дефис*.  Обязательный параметр. *Для необязательных параметров признак не указывается*.

Уровень переопределения: *Указывается, если уровень отличен от `common`. Для `common` – удаляется*.

<Описание>

<Пример>


