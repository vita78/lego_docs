# button2 

[Github](https://github.yandex-team.ru/lego/islands-components/blob/karamadjong%2Bpersidskiy.ISLCOMPONENTS-1287/common.blocks/button2/button2.ru.md)

**Статус блока**: в разработке.

Блок предоставляет компонент интерфейса для создания различных типов кнопок. 

Основные отличия от предыдущей версии блока [button](#): 


## Обзор

### Модификаторы блока

Модификатор | Допустимые значения | Способы установки | Описание
--- | --- | --- | ---
[type](#_type) |  `link`, `submit`, `check`, `radio` | `BEMJSON` | Тип кнопки. 
[theme](#_theme) | `normal`, `pseudo`, `action`, `clear` | `BEMJSON`, `JS` | **Обязательный**. Стилевое оформление.
[size](#_size) | `xs`, `s`, `m` | `BEMJSON`, `JS` | **Обязателный**. Размер кнопки.
[pin](#_pin) | `round-clear`, `clear-round`, `round-brick`, `brick-round`, `brick-clear`, `clear-brick`, `brick-brick`, `clear-clear` | `BEMJSON`, `JS` | Форма уголков кнопки.
[width](#_width) | `auto`, `max` | `BEMJSON`, `JS` | Ширина блока.
[checked](#_checked_yes) |  `yes` | `BEMJSON`, `JS` | Кнопка вжата. Используется для кнопок с модификатором `type` в значении `check` и `radio`.
[disabled](#_disabled_yes) | `yes` | `BEMJSON`, `JS` | Неактивное состояние.
[focused](#_focused_yes) | `yes` | `JS` | Фокус на кнопке.
[pressed](#_pressed_yes) | `yes` | – | Кнопка нажата.
[hovered](#_hovered_yes) | `yes` | – | Наведение курсором.

### Специализированные поля блока

Поле | Тип | Описание
--- | --- | ---
[text](#Работа-с-текстом) | `String` | Текст кнопки (не путать с полем `content`). Сокращенная форма использования элемента `text`. 
[iconLeft](#Работа-с-иконками) | `BEMJSON` | Иконка кнопки слева от текста. 
[iconRight](#Работа-с-иконками) | `BEMJSON` | Иконка кнопки справа от текста. 
[icon](#Работа-с-иконками) | `BEMJSON` | Иконка кнопки. Рекомендуется для использования без текста. Формируется блоком [icon](#). Сокращенная форма использования элемента `icon`. 
`content` | `BEMJSON` | Переопределяет содержимое кнопки по умолчанию. Используется для  более гибкой настройки.
`name` | `String` | Значение атрибута `name`. Имя кнопки, предназначенное для ее идентификации обработчиком формы.
`val` | `String` | Значение атрибута `value`. Значение кнопки и одновременно надпись на ней.
`url` | `String` | Значение атрибута `href`. Используется для кнопок-ссылок (модификатор `type` со значением `link`).
`target` | `String` | Значение атрибута `target`. Используется  для кнопок-ссылок (модификатор `type` со значением `link`).
`title` | `String` | Значение атрибута `title`. Текст всплывающей подсказки.
`id` | `String` | Значение атрибута `id`. Уникальный идентификатор кнопки.
`tabIndex` | `String` | Значение атрибута `tabindex`. Последовательность перехода между контролами нажатием **Tab**.


### Элементы блока

Cпособы использования: `BEMJSON`.

| Элемент | Описание |
| --------| -------- |
| [text](#Работа-с-текстом) | Текст кнопки.  |
| [icon](#Работа-с-иконками)| Иконка кнопки. |

### Публичные технологии блока

Блок реализован в технологиях:

* `js`
* `bh.js`
* `bemhtml.js`
* `css`
* `svg`(?)

## Подробнее

Блок `button2` предоставляет расширенные возможности для создания кнопки. Позволяет изменять поведение, состояние, содержимое, размер и внешний вид кнопок.

Функциональность блока основана на стандартной кнопке `<button атрибуты>` и `<a>` для ссылок.

[deps.js](#)   
[Особенности реализации](#features). (*Якорь на опциональный раздел. Если особенностей нет, ссылка и раздел удаляются.*)

<a name="modifiers"></a>
### Модификаторы блока

<a name="modifiers-name"></a>
#### Модификатор `type`

Допустимые значения: `'link'`, `'submit'`, `'check'`, `'radio'`.

Значение по умолчанию: `-`. Обычная кнопка.

Способ использования: `BEMJSON`.

Зависимости на проекте: подключите модификатор `type` с нужными значениями в `shouldDeps`-зависимости файла `button2.deps.js`. (???)


<a name="modifiers-name-1"></a>
##### Кнопка-ссылка (модификатор `type` в значении `link`)

Используется для создания кнопки-ссылки, которая обеспечивает переход по адресу, указанному в поле [url]().
Для передачи  значений `url` и `target` в BEMJSON, можно использовать методы [getUrl()]() и setUrl() в JS.

Кнопка-ссылка, используется тег <a> вместо <button>. Можно передавать url и target в BEMJSON, использовать getUrl() и setUrl() в JS.

<Пример>

<a name="modifiers-name-2"></a>
##### Кнопка отправки формы (модификатор `type` в значении `submit`)

Используется для создания кнопки, обеспечивающей отправку данных на сервер. Кнопка такого типа всегда должна располагаться в форме.

<Пример>

<a name="modifiers-name"></a>
#### Модификатор `название модификатора`

Допустимое значение: `булево значение`. *Обратите внимание, булевы значения заключаются только в бэктики.*

Способ использования: `BEMJSON`, `JS`. *Выберите нужное значение.*

<Описание>

<Пример>

<a name="declfields"></a>
### Специализированные поля блока

<a name="declfields-field"></a>
#### Поле `название поля`

Тип: `String`, `Number`, `Array`, `Boolean`. *Выберите нужный тип данных. Для полиморфных полей типы разделяются знаком `|`*

Значение по умолчанию: *Если нет, ставится дефис*.  Обязательное поле. *Для необязательных полей признак не указывается*.

Уровень переопределения: *Указывается, если уровень отличен от `common`. Для `common` – удаляется*.

<Описание>

<Пример>

<a name="elems"></a>
### Элементы блока

<a name="elems-name"></a>
#### Элемент `название элемента`

Элемент предоставляет <*Выберите нужный вариант*>

        шаблон для ...
        набор специализированных полей для ...
        СSS-класс, реализующий ...
        JS-класс, реализующий ...

Элемент дополняет <*Выберите нужный вариант*>
        
        базовый блок набором методов (свойств) для ...

Обязательный элемент. *Для необязательных элементов признак не указывается*. 

Уровень переопределения: *Указывается, если уровень отличен от `common`. Для `common` – удаляется*.

Зависимости на проекте: Подключите <тип зависимости>–зависимость от элемента `имя элемента` [и необходимых значений] в файле `имя_сущности_для_которой_декларируется_зависимость.deps.js`. *Указывается для опциональных сущностей*
>*Типы зависимостей: `shouldDeps`, `mustDeps`(Выберите нужный вариант)*

<Описание> 

<Пример>

>*Описание доопределения/переопределения элемента и пример для нестандартного случая (при наличии)*

<a name="elems-name-fields"></a>
### Специализированные поля элементов блока

<a name="elems-name-fields-name"></a>
#### Специализированное поле `название поля` элемента `название элемента`

Тип: `String`, `Number`, `Array`, `Boolean`. *Выберите нужный тип данных.*

Значение по умолчанию: *Если нет, ставится дефис*. Обязательное поле. *Для необязательных полей признак не указывается*.

<Описание>

<Пример>

<a name="declparams"></a>
### JS-параметры блока

<a name="params-name"></a>
#### Поле `название параметра`

Тип: `String`, `Number`, `Array`, `Boolean`. *Выберите нужный тип данных. Для полиморфных параметров типы разделяются знаком `|`*

Значение по умолчанию: *Если нет, ставится дефис*.  Обязательный параметр. *Для необязательных параметров признак не указывается*.

Уровень переопределения: *Указывается, если уровень отличен от `common`. Для `common` – удаляется*.

<Описание>

<Пример>


<a name="features"></a>
### Особенности реализации 

> Опциональный раздел. Содержит важные особенности реализации. Может включать следующие пункты (*в виде маркированного списка*):

* Деградации блока в IE версии X.
* Клавиатурное управление
    
   >**Клавиша** – описание выполняемого действия.  
   **Клавиша** + **Клавиша** – описание выполняемого действия (*для сочетания клавиш*).
* ...